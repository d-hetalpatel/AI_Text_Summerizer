# Core app dependencies
streamlit==1.32.0
transformers==4.39.3
torch==2.2.2
textstat==0.7.3

# Optional but safe (sometimes auto-installed by transformers)
huggingface-hub==0.23.0
tokenizers==0.19.1
